代理模式
向外部对象提供可以访问/控制本对象的功能接口
例子：
java的aop面向切面编程、windows快捷方式、火车票代售、防火墙
理解说明：
在面向对象的系统中，某些对象由于一些原因（安全等）不能外部对象直接访问，因此在外部对象需要访问某个对象时给该对象添加一个访问层，
控制外部对象的访问权限等，这个访问层就是代理
优点：高拓展性、系统智能化
缺点：代理层过多会导致系统请求速度代价变大